# WebSocket Configuration (Laravel Reverb)
# Required for real-time auction updates via WebSocket

# Reverb App Key - Must match REVERB_APP_KEY in Laravel backend
# Get this from your Laravel .env file or Reverb configuration
VITE_REVERB_APP_KEY=your-reverb-app-key-here

# Reverb WebSocket Host
# Development: localhost
# Production: your-domain.com or ws.your-domain.com
VITE_REVERB_HOST=localhost

# Reverb WebSocket Port
# Development: 8000 (Nginx proxy to Reverb)
# Production: 443 (HTTPS/WSS via Nginx)
VITE_REVERB_PORT=8000

# Reverb Scheme - Controls ws:// vs wss://
# http  → ws://localhost:8000   (Development, unencrypted)
# https → wss://your-domain:443 (Production, encrypted)
VITE_REVERB_SCHEME=http

# API Configuration
VITE_API_BASE_URL=http://localhost:8000
